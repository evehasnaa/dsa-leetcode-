class Solution {
public:
    bool isAnagram(string s, string t) {
        /*
        sort(s.begin(),s.end());
        sort(t.begin(),t.end());
        return s==t;

     ] 
      map<char,int >freqmap;
       if (s.size()!=t.size())
       return false;
      for (int i=0;i<s.size();i++)
      {
        freqmap[s[i]]++;
        freqmap[t[i]]--;
      }
      
      for (auto count :freqmap)
      {
        if(count.second)
         return false;
          
      }
      return true;
      */
      int freqchar[27]={};
      for (auto x:s)
      {
        freqchar[x-'a']++;
      }
      for (auto x:t)
      {
        freqchar[x-'a']--;
      }
       for (auto x:freqchar)
      {
      if (x!=0)
      return false;
      }
      return true;
      



    } 
};