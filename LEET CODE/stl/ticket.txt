#include <iostream>
#include<queue>
using namespace std;

int main()
{
    queue<int >q;
    int t;
    cin>>t;
    while(t--)

    {
       int x,id;

       cin >>x>>id;
       if (x==1)
        q.push(id);
       else
       {

           if (!q.empty()&&q.front()==id)
            {
                   q.pop();
                  cout <<"YES"<<endl;
                  
               
            }

           else
            cout <<"NO"<<endl;
       }

    }
    return 0;
}
