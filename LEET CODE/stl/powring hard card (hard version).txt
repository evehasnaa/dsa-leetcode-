#include <iostream>
#include<vector>
#include<set>
using namespace std;

void init(){
    cin.tie(0);
    cin.sync_with_stdio(0);
}
int main()
{   init();
/*
    multiset<int>s={1,2,3,4,2,1};
    cout<<s.count(5)<<endl;
    for (auto it:s)
    {
        cout <<it<<" ";
    }
     cout <<endl;
     int it3=*s.find(4);
     s.erase(it3);
     for (auto it2:s)
    {
        cout <<it2<<" ";
    }

*/
    int t;
    cin>>t;
    while(t--)
    {
        multiset<long long,greater<>>s;
        int n;
        cin>>n;
        long long x;
        long long  res=0;
        for (int i=0;i<n;i++)
        {
            cin>>x;
                if (x!=0)
                    s.insert(x);
                else
               {   if (s.size()>0){

                    res+=*s.begin();
                    s.erase(s.begin());
                    }
               }
            }
        cout <<res<<endl;


    }

    return 0;
}
