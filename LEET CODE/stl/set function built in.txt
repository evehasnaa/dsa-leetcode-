#include <iostream>
#include<vector>
#include<set>
using namespace std;
/*
int main()
{
    int n,m;
    cin>>n>>m;
    vector<int >arr(n);
    for (int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    int sum=0;
    for (int i=0;i<m;i++)
        sum+=arr[i];
    int mx=sum;
    for (int i=m;i<n;i++)
    {
        sum+=arr[i];
        sum-=arr[i-m];
        mx=max(mx,sum);
    }

    cout << " max sum is :"<<mx<<endl;
    return 0;
}
*/
int main()
{
    set<int >s;
     s.insert(1);
     s.insert(9);
     s.insert(3);
     s.insert(7);
     s.emplace(3);

    for (auto it:s)
    {
        cout <<it<<endl;
        it++;
    }
   // s.erase(s.begin(),s.end());
    int it1=s.count(0);

    cout <<"**"<<it1<<endl;
    int it2 = *s.find(9);
    cout << "##"<<it2<<endl;
    //another method for print set
    for (auto it=s.begin();it!=s.end();it++)
        cout <<*it<<" ";
    cout <<endl;
   cout <<"*#*" <<*s.lower_bound(9)<<endl;
   cout <<"*#*" <<*s.upper_bound(9)<<endl;

    return 0;
}
