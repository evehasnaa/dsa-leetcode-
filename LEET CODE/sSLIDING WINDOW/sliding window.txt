#include <iostream>
#include<string>
using namespace std;
/*
         int string_score( string t){
             string s= t;

             int cntr =0;

             for (int i=0;i<t.size();i++){
                if (t[i]=='V'){
                    cntr+=5;
                }
                else if (t[i]=='W'){
                    cntr+=2;
                }
                else if (t[i]=='X'){
                    t=t.erase(0,i);
                }
                else if (t[i]=='Y'){

                    (t[i+1],t[i-1]);
                }
                else if (t[i]=='Z'){
                        if (t[i+1]=='V'){
                            cntr/=5;
                        }
                        else if (t[i+1]=='W'){
                            cntr/=2;
                        }
                        else
                          t=t.erase(0,i+1);


                }

             } return cntr;

         }
int main()
{
    int size_string ;
    cin>>size_string;

    string str;
    cin>>str;

    cout << string_score(str)<<endl;


    return 0;
}






























bool isVowel (char ch){
    return (ch=='a' || ch=='u' ||ch=='i' || ch=='e' || ch== 'o' );
}

class Solution {
public:
    int maxVowels(string s, int k) {
        int sz=k;
        int count =0;
        for (int i=0;i<sz;i++){
            if (isVowel(s[i])){
                count++;
             }
          }int  mx=count;
             int remv=0;
             for (int add=sz; add<s.size();add++){
                if (isVowel(s[remv])){
                    count--;
                }
                if (isVowel(s[add])){
                   count++;

                   remv++;
                   mx=max(mx,count);
                }
             }
         return mx ;
    }
};









*/
      int main (){
          int n=6;


          int arr[n]={1,2,5,5,5,1};

          int sum=0;
          int k=3;
          int mx;

          for (int i=0;i<n;i++){

            sum+=arr[i];

          }int sm=sum;
           int remv=0;
            for (int j=k;j<n;j++){

                sum+=arr[j];
                sum-=arr[remv];
                remv++;
                mx=max(sm,sum);



          }cout <<mx<<endl;



        return 0;
}
